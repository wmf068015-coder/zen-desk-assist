# PRD：客服工作台信息面板与基础能力优化

版本：V1.0  
状态：待评审

## 1. 概要

优化客服工作台右侧信息面板，让客服可以快速查看用户浏览商品、核验订单、翻译消息并记录交接信息。同时补充聊天视频直接播放和登录 Token 24 小时有效期能力。

## 2. 联系人

| 角色   | 负责人 | 职责                     |
| ------ | ------ | ------------------------ |
| 产品   | 待定   | 需求确认与验收           |
| 设计   | 待定   | 交互与视觉规范           |
| 前后端 | 待定   | 功能实现与接口联调       |
| 测试   | 待定   | 正常、边界和异常场景验证 |

## 3. 背景

客服处理会话时需要在聊天、商品、订单和外部工具之间频繁切换，影响响应效率。视频附件还需要下载后查看，交接信息和登录有效期也缺少统一规则，因此需要将常用信息和操作集中到工作台内。

## 4. 目标

- 客服在当前会话内完成商品发送、订单核验、中文翻译和交接记录。
- 用户发送的视频可直接播放，不要求客服先下载。
- 每次成功登录都签发新的 Token，并从登录时起固定有效 24 小时。

关键结果：

- 商品、订单、翻译和便签操作均可在右侧面板完成。
- 支持格式的视频在线播放成功率不低于 99%。
- 过期或无效 Token 的拦截率为 100%，前端统一回到登录页。

## 5. 目标用户

- 一线客服：高频处理咨询、订单和售后问题。
- 客服主管：查看交接记录并保证服务连续性。
- 系统管理员：维护登录安全和媒体访问规则。

## 6. 用户价值

- 减少页面切换和重复查询，缩短首次响应时间。
- 订单信息按用户已提供的身份信息展示，降低误查风险。
- 视频、翻译和交接信息集中在会话内，提升处理连续性。
- Token 有明确有效期，兼顾使用连续性与账号安全。

## 7. 方案

### 7.1 页面结构

- 右侧面板顶部保留用户联系信息。
- “当前浏览”和“订单信息”左右并列为两个标签，默认选中“当前浏览”。
- AI 翻译和交接便签仅显示在“当前浏览”标签内。
- “订单信息”标签只展示订单相关内容。

### 7.2 功能要求

#### F1 当前浏览与商品发送

正常流程：

1. 展示用户最近浏览的最多 3 个商品，包括首图、商品名和链接。
2. 客服点击发送按钮，将商品名和链接发送到当前会话。

边界：

- 超过 3 条时只取最近 3 条；无浏览商品时展示当前页面链接。
- 已结束、已超时或已挂起会话不可发送商品。

异常处理：

- 图片加载失败时显示默认商品图，不影响链接发送。
- 发送失败时保留当前页面状态，并提示“发送失败，请重试”。

#### F2 订单信息

正常流程：

1. 系统从用户表单或客户消息中识别订单号和邮箱。
2. 两项信息均存在且与订单匹配后，展示商品项目、数量、单价、总价和物流信息。

边界：

- 缺少订单号或邮箱、邮箱不匹配、订单不存在时，显示“暂无订单信息”。
- 订单存在但无物流数据时，物流信息显示 `--`。
- 不展示其他用户或未被当前用户暴露的订单。

异常处理：

- 订单接口超时或失败时显示“订单信息加载失败”，支持重试。
- 订单数据字段缺失时只隐藏缺失字段，不影响其他有效字段展示。

#### F3 AI 翻译

正常流程：

1. 客服输入文字并点击“翻译成中文”。
2. 系统自动识别语言并返回中文结果，原文保留直到客服清空。

边界：

- 单次最多 500 字；纯中文内容直接返回原文。
- 空内容不可提交；切换会话后清空未保存的翻译内容。

异常处理：

- 无法识别语言时提示调整文字后重试。
- 翻译服务超时或失败时不清空原文，并提供重试入口。

#### F4 交接便签

正常流程：

1. 客服在“当前浏览”标签内填写便签并保存。
2. 便签按会话保存，下一位客服进入同一会话时可查看。

边界：

- 最多 500 字；空内容保存视为清空便签。
- 切换到“订单信息”标签后不显示便签。
- 不同会话之间的便签不得串用。

异常处理：

- 保存失败时保留输入内容并提示重试。
- 读取失败时显示“便签加载失败”，不得展示其他会话的数据。

#### F5 视频直接播放

正常流程：

1. 用户发送视频后，聊天记录中展示内嵌播放器和视频封面。
2. 客服点击播放即可观看，可暂停、调整进度、音量和全屏，无需先下载。

边界：

- V1 支持 MP4（H.264/AAC）和 WebM；其他格式需转码后播放。
- 默认不自动播放、不自动开启声音，仅预加载视频元信息。
- 播放地址必须校验当前会话权限，并支持 Range 请求以便拖动进度。

异常处理：

- 视频上传或转码中显示处理状态，不允许播放。
- 网络中断时显示“加载失败”，支持从原进度重试。
- 格式不支持、文件损坏或转码失败时提示重新上传；可保留受权限控制的下载作为降级入口。
- 播放链接过期时自动申请新链接并重试一次，仍失败则提示刷新。

#### F6 登录 Token 24 小时刷新

正常流程：

1. 用户通过账号校验后，服务端签发新的访问 Token。
2. Token 的签发时间为本次登录时间，过期时间固定为签发后 24 小时。
3. 24 小时内正常访问；到期后下一次请求返回未登录状态，前端清理会话并跳转登录页。

边界：

- 普通接口调用和页面刷新不会延长 Token 有效期。
- 同一设备再次登录时签发新 Token，并使该设备旧 Token 失效。
- 不同设备会话相互独立；退出登录只注销当前会话，管理员强制下线除外。
- 有效期以服务端时间为准，不依赖客户端时间。

异常处理：

- 账号或验证码错误时不签发 Token，并返回明确错误原因。
- Token 过期、伪造、撤销或签名错误时统一返回 `401`，错误码分别为 `TOKEN_EXPIRED` 或 `TOKEN_INVALID`。
- 登录接口超时或失败时不得写入半成品 Token，用户可重新登录。
- 页面有未提交内容时，Token 过期前先暂存本地草稿；重新登录后由用户决定是否恢复。

### 7.3 技术约束

- Token 建议存放于 `HttpOnly + Secure + SameSite` Cookie，不写入可被脚本读取的存储。
- 视频文件和播放地址必须鉴权，禁止跨会话访问；媒体服务需支持分段加载。
- 订单查询必须同时校验订单号、邮箱和当前租户。
- 生产环境的便签应保存到服务端，前端本地存储仅用于 Demo。

### 7.4 假设

- 订单、翻译、媒体和认证服务均提供可用接口。
- 用户上传视频前已通过文件类型、大小和安全扫描。
- 多设备登录默认允许，后续可增加“仅允许单设备登录”策略。

## 8. 发布与验收

首版范围：右侧标签、商品发送、订单展示、AI 翻译、交接便签、视频内嵌播放、24 小时 Token。后续版本再增加视频转码进度、更多格式和单设备登录策略。

验收要求：

- 每项功能覆盖正常流程、边界和异常用例。
- 不得出现跨会话订单、便签、视频或 Token 数据泄漏。
- 桌面端主流浏览器中布局无重叠，视频可播放和拖动进度。
- Token 在签发后 24 小时内有效，到期后无法继续调用受保护接口。
