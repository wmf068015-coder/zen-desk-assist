# PRD：客服工作台商品检索与消息提醒

版本：V1.0  
状态：待评审

## 1. 概要

本次优化增加独立站商品检索、回到最新消息和新消息提醒。目标是减少客服查找商品和追踪新消息的操作成本，且不改变现有会话、订单及用户信息流程。

## 2. 联系人

| 角色 | 负责人 | 职责                     |
| ---- | ------ | ------------------------ |
| 产品 | 待定   | 需求确认与验收           |
| 设计 | 待定   | 交互与视觉规范           |
| 开发 | 待定   | 数据、消息与通知能力实现 |
| 测试 | 待定   | 正常、边界及异常场景验证 |

## 3. 背景

客服需要快速检索并发送独立站商品；查看历史消息时也容易错过最新内容。现有工作台缺少商品统一入口、返回最新消息入口和离页提醒，因此补充以上能力。

## 4. 目标

- 客服可在当前会话内完成商品检索和快捷发送。
- 客服查看历史消息时可一键返回最新消息，且页面不强制跳动。
- 当前会话不可见时，客户新消息能够及时提醒客服。

## 5. 目标用户

- 一线客服：处理售前咨询、商品推荐和持续会话。
- 客服主管：关注响应及时性和消息漏接情况。

## 6. 范围

本期包含：商品检索、销量列表、分页、快捷发送、回到最新消息、应用内提醒及浏览器系统通知。

本期不包含：商品编辑、库存管理、复杂筛选、通知中心、短信或邮件提醒。

## 7. 功能需求

### F1 独立站商品检索与发送

#### 正常流程

1. 右侧面板提供“当前浏览”“订单信息”“商品检索”三个同级标签。
2. 客服进入“商品检索”后，可按商品名称、SKU 或预设关键词搜索。
3. 无搜索词时，按销量从高到低展示前 50 个商品；每页 10 个，共 5 页。
4. 每个商品展示销量排名、首图、商品名称、商品链接和销量。
5. 客服点击发送按钮后，商品名称和链接发送到当前会话，访客端可查看。

#### 边界

- 搜索不区分 SKU 英文字母大小写，自动忽略首尾空格。
- 搜索仅匹配商品名称、SKU 和预设关键词；商品链接不参与匹配。
- 搜索词最长 80 个字符；输入变化后自动回到第 1 页。
- 搜索结果超过 10 条时分页展示；不足 10 条时不补空位。
- 无结果时显示“未找到相关商品”，页码保持“第 1 / 1 页”。
- 第一页禁用“上一页”，最后一页禁用“下一页”。
- 已结束、已超时或已挂起的会话不可发送商品。

#### 异常处理

- 商品图片加载失败时显示默认图，不影响名称和链接展示。
- 商品数据加载失败时显示“商品加载失败”，保留重试入口。
- 发送失败时不清空搜索条件和当前页，并提示“发送失败，请重试”。
- 商品已下架或链接失效时禁止发送，并提示商品不可用。

### F2 回到最新消息

#### 正常流程

1. 打开或切换会话时，消息区默认定位到最新消息。
2. 客服向上浏览历史记录，且最新消息完全离开可视区域后，显示“回到最新消息”按钮。
3. 客服点击按钮后，消息区平滑滚动到最底部，按钮自动隐藏。
4. 客服停留在最新消息处时，新消息到达后自动跟随到底部。
5. 客服正在查看历史记录时，新消息不得强制改变其阅读位置。

#### 边界

- 最新消息完整可见或仅部分可见时，均不显示按钮。
- 只有最新消息完全不可见时才显示按钮。
- 无消息、仅一条消息或消息不足一屏时不显示按钮。
- 最新消息为文字、图片、视频、文件、表单或系统消息时，判断规则一致。
- 切换会话后清除上一会话的按钮状态，并重新定位到新会话底部。

#### 异常处理

- 无法读取最新消息位置时，按钮默认隐藏，不阻断消息浏览。
- 点击后因消息重新加载未能定位时，重新计算消息位置并再尝试一次。
- 浏览器不支持可见区域监听时，降级为按消息区滚动位置判断。

### F3 客户新消息提醒

#### 正常流程

1. 客服首次操作工作台时，系统申请浏览器通知权限。
2. 新增消息发送方为客户，且对应会话不是当前可见会话时，显示应用内弹窗。
3. 弹窗展示客户名称、消息摘要和“查看”按钮。
4. 客服点击“查看”后，进入对应会话并定位到最新消息。
5. 当浏览器标签不可见或窗口失焦，且通知权限已授权时，同时发送系统通知。

#### 边界

- 当前会话在前台可见且窗口有焦点时，不弹提醒，消息直接进入记录。
- 当前会话可见但客服正在查看历史记录时，不弹提醒，仅显示“回到最新消息”。
- 新消息来自其他会话时，工作台前台仅显示应用内弹窗；页面失焦时可追加系统通知。
- 客服、AI 和系统消息不触发客户新消息提醒。
- 同一消息 ID 只提醒一次；同一会话连续系统通知可按会话合并。
- 浏览器通知被拒绝或不受支持时，仅使用应用内弹窗，不重复申请权限。
- 浏览器关闭、网络断开或用户退出登录后，本期不保证系统通知送达。

#### 异常处理

- 通知权限申请失败时不得影响聊天功能，并静默降级为应用内弹窗。
- 系统通知创建失败时保留应用内弹窗，并记录失败日志。
- 点击提醒时会话已结束，仍可进入只读记录；会话不存在时提示“会话已失效”。
- 消息重复、乱序或重连补发时，按消息 ID 去重，不重复提醒。
- 消息摘要解析失败时，统一显示“收到一条新消息”。

## 8. 验收与发布

### 验收标准

- 商品名称、SKU、关键词可命中，商品链接搜索结果为 0。
- 默认销量列表最多 50 条，每页 10 条，排序和页码正确。
- 最新消息仍有任意部分可见时按钮不出现，完全不可见后才出现。
- 点击“回到最新消息”后定位到底部并隐藏按钮。
- 客户新消息仅在当前会话不可见时提醒，客服、AI 和系统消息不误触发。
- 通知权限拒绝或浏览器不支持时，聊天和应用内提醒仍可正常使用。

### 发布建议

首版随客服工作台发布。上线后重点观察商品发送成功率、新消息提醒到达率、重复提醒率和客服首次响应时间。
