# PRD-工单处理与管理员分配

## 1. 背景与目标

访客工单留言、service/support 邮箱来信需要在客服工作台统一处理。客服需要查看完整往来、回复或转发邮件、预览附件，并准确维护消息状态。管理员需要查看客服工作量，并在大量工单下快速分配处理人。

本期目标：

*   统一管理访客留言和客服邮箱来信。

*   支持客服完成工单查看、邮件回复、邮件转发、附件预览、失败重试和处理状态更新。

*   支持管理员查看客服处理概览，并单条或批量分配工单。

*   通过筛选、内部滚动和固定分页支持每日约 600 条工单。


## 2. 范围

本期包含：

1.  工单列表、筛选、排序和状态展示。

2.  回复链、邮件回复、抄送、转发、富文本和附件预览。

3.  发送失败后的快捷重发和重新编辑。

4.  持续处理和处理完毕操作。

5.  管理员客服处理概览、工单筛选、单条分配和批量分配。


本期不包含：

1.  管理员账号类型、模块权限和客服在线状态控制。

2.  工单优先级维护。

3.  物流配送标签。


## 4. 工单模块

### 4.1 工单列表、筛选与排序

#### 4.1.1 功能规则

每条工单展示来源、收件日期、主题、买家邮箱、处理人、阅读状态、处理状态和回复状态。

来源包括工单留言、service 邮箱和 support 邮箱。筛选包括：

*   时间：近 7 天、近 1 个月、近 3 个月、近 6 个月、自定义时间。

*   来源：全部来源、工单留言、service 邮箱、support 邮箱。

*   处理人：全部处理人、未分配、指定处理人。

*   状态：全部、已读、未读、持续处理、处理完毕、发送失败。

*   关键词：工单号、主题、买家邮箱、客户名称、订单号和正文。

默认按最近一封客户来信的收件时间由近到远排列。

#### 4.1.2 正常流程

1.  客服进入工单模块，系统加载默认时间范围和全部来源工单。

2.  系统按收件时间倒序展示列表，并计算当前筛选结果总数。

3.  客服选择来源、处理人、状态或时间范围，列表实时刷新。

4.  客服输入关键词，系统在工单号、主题、邮箱、订单号和正文中匹配。

5.  客服点击工单进入详情，列表保留当前筛选条件。

#### 4.1.3 边界规则

*   自定义时间包含开始日和结束日全天。

*   来源筛选必须区分工单留言、service 邮箱和 support 邮箱。

*   筛选无结果时显示空状态，总数为 0，不显示有效分页。

*   列表不展示优先级和物流配送标签。

#### 4.1.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 工单数据加载失败 | 保留已加载数据或显示空状态，并提示加载失败 | 刷新或稍后重试 |
| 自定义日期为空 | 不执行查询并提示补全日期 | 补全开始日期和结束日期 |
| 开始日期晚于结束日期 | 不执行查询并显示日期错误 | 修正日期范围 |
| 关键词查询失败 | 保留原列表并提示查询失败 | 修改关键词或重试 |

#### 4.1.5 验收标准

*   列表字段、来源筛选、处理人筛选、状态筛选和时间筛选均准确。

*   默认排序为最近客户来信在前，筛选后仍保持该排序。

*   日期错误、空结果和加载失败均有明确反馈。


### 4.2 回复链与阅读状态

#### 4.2.1 功能规则

*   每个独立问题对应一个 `ticket_id` 和一条 `thread_id`。

*   回复链按时间正序展示客户来信、客服回复和转发记录。

*   每条消息展示来源、发件人、收件人、抄送、主题、正文、时间、发送结果和附件。

*   来源为工单留言时展示“工单留言”；来源为邮箱来信时展示对应邮箱。

阅读状态和其他状态独立管理：

| 状态维度 | 状态 | 规则 |
| --- | --- | --- |
| 阅读状态 | 已读、未读 | 新客户消息为未读；客服打开并加载工单后为已读 |
| 处理状态 | 持续处理、处理完毕 | 默认持续处理，可不回复直接处理完毕 |
| 回复状态 | 未发送、已发送、发送失败 | 以最近一封非转发邮件的发送结果为准 |

#### 4.2.2 正常流程

1.  客服点击列表工单，系统加载完整回复链。

2.  回复链加载成功后，工单阅读状态变为已读，其他状态保持不变。

3.  客户新邮件进入原回复链后，工单更新时间刷新并变为未读、未发送。

4.  客服切换工单时，当前工单的阅读状态保留，下一封工单按自身状态展示。

#### 4.2.3 边界规则

*   同一客户邮箱存在多个主题时，必须按线程标识、主题和邮件头区分，不得仅按邮箱合并。

*   相同外部邮件 ID 只能写入一次，不得重复创建消息或增加未读数。

*   客服打开工单只改变阅读状态，不得自动改变处理状态和回复状态。

#### 4.2.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 回复链加载失败 | 不将工单标记为已读，保留列表未读提示 | 重试加载 |
| 邮件同步失败 | 保留已有回复链，提示同步失败 | 稍后刷新或重试 |
| 重复邮件或重复回调 | 按外部邮件 ID 幂等处理 | 无需重复操作 |
| 回复链匹配失败 | 创建新工单，不覆盖已有回复链 | 按新工单处理 |

#### 4.2.5 验收标准

*   客服打开工单后未读提示消失，处理状态和回复状态不被重置。

*   客户新回复可以进入原回复链，并正确恢复未读和未发送。

*   重复同步不会生成重复消息或重复计数。


### 4.3 邮件回复框

#### 4.3.1 功能规则

*   回复框默认收起，点击可展开或收起，收起不清空草稿。

*   支持编辑收件人、抄送和主题。

*   正文支持换行、正文、标题 1/2/3、字体、加粗、斜体、下划线、对齐、项目符号、编号列表和链接。

*   回复框内直接展示当前回复链的往来附件。

*   最多添加 5 个回复附件。

#### 4.3.2 正常流程

1.  客服展开回复框，系统载入当前工单默认收件人和主题。

2.  客服填写正文、抄送和附件，并可调整字体和格式。

3.  系统校验收件人、抄送、主题和正文后发送邮件。

4.  发送成功后，客服邮件追加到回复链，回复状态变为已发送。

5.  系统清空已发送草稿并收起回复框，处理状态保持持续处理。

#### 4.3.3 边界规则

*   收件人、主题或正文为空时禁止发送。

*   抄送邮箱必须全部合法，多个邮箱使用约定分隔符输入。

*   附件最多 5 个，超过数量不得继续添加。

*   切换工单不得串用其他工单草稿。

*   处理完毕工单的回复框不可编辑和发送。

#### 4.3.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 收件人或抄送地址非法 | 禁止发送并指出非法地址 | 修正地址后重试 |
| 主题或正文为空 | 禁止发送并定位缺失字段 | 补全内容后重试 |
| 发送接口失败 | 标记发送失败，保留全部草稿内容 | 快捷重发或重新编辑 |
| 发送超时 | 先查询服务端最终结果，确认前禁止重复发送 | 等待结果后再操作 |
| 邮件退信 | 以服务端结果覆盖前端状态并显示原因 | 修改地址或内容后重发 |

#### 4.3.5 验收标准

*   回复框可以展开/收起，草稿、换行和富文本格式不丢失。

*   校验失败时不能发送，发送失败时内容和附件不丢失。

*   发送成功后回复链、回复状态和草稿状态同步更新。


### 4.4 邮件转发

#### 4.4.1 功能规则

*   点击“转发”后弹出转发地址选择框。

*   支持选择已有转发地址或新增合法地址。

*   默认转发当前回复链最后一条消息，并保留其附件。

*   转发记录追加到回复链，但不改变客户原邮箱和正常回复状态。

#### 4.4.2 正常流程

1.  客服点击转发，系统打开地址选择框。

2.  客服选择已有地址，或输入合法邮箱并保存为新地址。

3.  系统展示待转发的主题、来源、时间和附件数量。

4.  客服确认后发送，转发结果追加到回复链。

#### 4.4.3 边界规则

*   转发地址不能为空且必须为合法邮箱。

*   处理完毕工单不可直接转发，必须先标记持续处理。

*   取消弹窗不得产生转发记录，不得改变回复状态。

*   没有消息时不允许发起转发。

#### 4.4.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 转发地址非法 | 禁止确认发送并提示地址错误 | 修正或新增地址 |
| 转发发送失败 | 保留转发内容和附件，记录发送失败 | 快捷重发或重新编辑 |
| 最后一条消息加载失败 | 不打开可发送状态，提示消息加载失败 | 重试加载后再转发 |

#### 4.4.5 验收标准

*   转发可以取消、选择已有地址和新增地址。

*   转发邮件携带最后一条消息附件，并正确显示在回复链中。

*   转发失败时内容不丢失，取消时不产生记录。

### 4.5 附件预览

#### 4.5.1 功能规则

*   图片在当前页面预览。

*   视频在当前页面直接播放，不要求先下载。

*   PDF在当前页面打开预览。

*   其他格式展示文件名、大小和类型，并提供可用的打开/下载入口。

*   预览弹窗可关闭，关闭不影响草稿和工单状态。

#### 4.5.2 正常流程

1.  客服在回复链或回复框中点击附件。

2.  系统根据文件类型选择图片、视频、PDF或通用预览方式。

3.  视频显示播放控件，客服可直接播放和暂停。

4.  客服关闭预览后返回原工单。

#### 4.5.3 边界规则

*   当前消息没有附件时不展示附件区域。

*   文件没有预览地址时不能伪造可预览状态。

*   大文件预览不得阻塞正文和其他消息查看。

#### 4.5.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 预览地址失效 | 提示预览失败，保留文件信息 | 关闭预览，必要时打开/下载文件 |
| 视频加载失败 | 显示播放失败提示，不自动下载 | 重试或使用文件入口 |
| PDF加载失败 | 显示文件信息和降级入口 | 打开或下载原文件 |
| 文件类型未知 | 使用通用文件预览 | 通过打开/下载入口查看 |

#### 4.5.5 验收标准

*   图片、视频和 PDF 均可在当前页面预览，视频无需下载即可播放。

*   不支持的附件有明确降级处理，不影响消息正文查看。

*   关闭预览不会清空草稿或改变工单状态。

### 4.6 工单处理状态

#### 4.6.1 功能规则

回复框底部提供同一层级的两个操作：

*   标记持续处理：不发送邮件，处理状态改为持续处理。

*   标记处理完毕：不发送邮件，处理状态改为处理完毕，不改变回复状态。

处理完毕后禁止回复和转发；标记持续处理后恢复。

#### 4.6.2 正常流程

1.  客服查看工单并判断是否需要继续跟进。

2.  需要继续跟进时点击标记持续处理。

3.  无需发送邮件即可结束时点击标记处理完毕。

4.  后续需要继续处理时，点击标记持续处理恢复回复和转发。

#### 4.6.3 边界规则

*   标记处理完毕不发送邮件，不改变已发送/未发送/发送失败状态。

*   已处于目标状态时重复点击，不重复写入状态记录。

*   标记处理状态不改变阅读状态和处理人。

#### 4.6.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 状态保存失败 | 保留原处理状态并提示失败 | 重试状态操作 |
| 状态更新冲突 | 拒绝静默覆盖，刷新为服务端最新状态 | 刷新后重新操作 |
| 工单已被其他客服处理 | 提示状态已更新，禁用过期操作 | 查看最新状态后决定是否恢复 |

#### 4.6.5 验收标准

*   客服可以不发送邮件直接标记处理完毕。

*   处理完毕后回复和转发不可用，恢复持续处理后重新可用。

*   处理状态变更不影响阅读状态和回复状态。

## 5. 管理员工单分配与处理

### 5.1 客服处理概览

#### 5.1.1 功能规则

展示全部 13 个可分配客服，不展示在线状态：

*   在线客服账号：Marzia、Kevin、Flora、Christiana、罗凤、张海鸥。

*   邮件客服账号：吴金香、曾芬、余雪、徐晓芬、郑绮淇、曹超议、肖琼。


每位客服展示已分配工单数、持续处理数、处理完毕数、未读数和完成率。完成率 = 处理完毕数 / 已分配工单数；已分配数为 0 时显示 0%。概览统计全部工单，不随右侧筛选改变。

#### 5.1.2 正常流程

1.  管理员进入工单分配模块。

2.  系统加载 13 个客服及其当前工单统计。

3.  管理员分配或取消分配工单后，概览中的对应数据刷新。

4.  客服处理或完结工单后，概览中的处理数、未读数和完成率刷新。

#### 5.1.3 边界规则

*   13 个客服均需展示，即使当前没有分配工单。

*   已分配数为 0 时完成率显示 0%，不得显示 NaN 或空值。

*   概览不显示在线状态，不受右侧时间和处理人筛选影响。

#### 5.1.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 概览数据加载失败 | 显示加载失败，不展示过期统计为最新数据 | 刷新或稍后重试 |
| 统计数据为空 | 保留客服名单，各项数量显示 0 | 正常使用分配功能 |
| 分配结果同步失败 | 保留旧统计，提示数据未同步 | 重试分配或刷新 |

#### 5.1.5 验收标准

*   13 个客服全部展示，统计字段计算正确。

*   分配、取消分配、处理完毕和未读变化后统计可以同步更新。

*   页面不显示在线状态字段。

### 5.2 工单分配列表、筛选与分页

#### 5.2.1 功能规则

筛选条件包括：

*   收件时间：全部时间、近 24 小时、近 7 天、近 1 个月、近 3 个月、近 6 个月、自定义时间。

*   处理人：全部处理人、未分配、指定客服。

*   关键词：工单号、主题、买家邮箱和处理人。

列表默认按收件时间由近到远排列，每页固定 50 条。

#### 5.2.2 正常流程

1.  管理员打开分配列表，系统加载默认全部时间和全部处理人。

2.  系统按收件时间倒序展示工单。

3.  管理员选择时间、处理人或输入关键词，列表刷新并回到第 1 页。

4.  结果超过 50 条时，管理员通过上一页/下一页查看其他结果。

5.  当前页达到 20 条后，只有列表区域内部滚动，页面整体不被拉长。

#### 5.2.3 边界规则

*   每页最多 50 条，不提供其他分页大小选项。

*   超过 50 条只展示页码和总数，不展示“第 X-Y 条”。

*   当前页达到 20 条后启用内部滚动。

*   筛选无结果时显示空状态，总数为 0，分页按钮隐藏或禁用。

*   切换筛选或关键词时清空已选工单并回到第 1 页。

#### 5.2.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 分配列表加载失败 | 保留已加载数据或显示空状态并提示失败 | 刷新或稍后重试 |
| 自定义日期错误 | 不执行查询并展示错误 | 修正日期 |
| 当前页超出筛选结果 | 自动回到最后一页 | 继续查看结果 |
| 筛选结果为空 | 显示空状态，不保留无效分页 | 修改筛选条件 |

#### 5.2.5 验收标准

*   时间、处理人和关键词筛选准确，结果按收件时间倒序。

*   每页固定 50 条，20 条后仅列表内部滚动。

*   分页只显示总数和页码，不显示区间条数。

### 5.3 单条分配

#### 5.3.1 功能规则

*   每条工单提供处理人下拉框。

*   可选择 13 个客服中的任意一人，或选择“未分配”。

*   同一工单同一时刻只能有一个处理人。

#### 5.3.2 正常流程

1.  管理员在工单行打开处理人下拉框。

2.  管理员选择目标客服并确认。

3.  系统保存工单 ID、处理人和更新时间。

4.  当前行、客服处理概览和客服工作台同步显示新处理人。

5.  选择“未分配”时清除当前处理人。

#### 5.3.3 边界规则

*   未选择有效处理人时不能保存。

*   取消分配不删除工单，不改变工单的阅读、处理和回复状态。

*   重复选择当前处理人不得重复生成无意义的变更记录。

#### 5.3.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 保存失败 | 保留原处理人，不显示为已保存 | 重试保存 |
| 处理人已失效 | 禁止保存并提示处理人不可用 | 选择其他处理人 |
| 并发修改 | 按服务端版本拒绝覆盖并提示数据已更新 | 刷新后重新分配 |

#### 5.3.5 验收标准

*   单条分配和取消分配结果准确、可刷新恢复。

*   分配结果同步到工单列表、客服概览和客服工作台。

*   保存失败时不出现假成功。

### 5.4 批量分配

#### 5.4.1 功能规则

*   每行提供选择框，支持全选当前页。

*   批量分配目标可以是任意一个客服或“未分配”。

*   全选只作用于当前页，已选项数量需要明确展示。

#### 5.4.2 正常流程

1.  管理员勾选一条或多条当前页工单。

2.  管理员选择目标客服或“未分配”。

3.  点击批量分配，系统逐条保存分配结果。

4.  全部成功后清空选择，并刷新列表和客服处理概览。

#### 5.4.3 边界规则

*   没有勾选工单时，批量分配按钮不可用。

*   切换筛选、关键词或页面时，当前选择清空。

*   批量分配不改变工单阅读状态、处理状态和回复状态。

#### 5.4.4 异常处理

| 异常 | 系统处理 | 用户操作 |
| --- | --- | --- |
| 批量保存全部失败 | 保留原处理人并提示失败 | 重试批量分配 |
| 批量保存部分失败 | 成功项保留，返回失败工单明细 | 只重新选择失败项 |
| 批量操作超时 | 查询服务端结果，未确认前不重复提交 | 等待结果或刷新后核对 |

#### 5.4.5 验收标准

*   当前页全选、取消全选和批量取消分配可用。

*   批量成功后选项清空，相关页面数据一致。

*   部分失败时成功项和失败项状态清晰，不丢失失败项。

## 6. 测试要点

### 6.1 工单模块

*   验证三类来源展示和筛选准确。

*   验证工单列表字段、关键词、时间边界和默认排序。

*   验证打开工单、客户新回复、重复同步和回复链匹配。

*   验证回复框展开/收起、换行、标题、字体、加粗、下划线和其他富文本格式。

*   验证收件人、抄送、主题、正文校验及发送成功、失败、超时和退信。

*   验证转发取消、地址新增、地址校验、附件携带和失败重试。

*   验证图片、视频、PDF预览及未知附件降级处理。

*   验证处理完毕无需发送邮件，恢复持续处理后回复/转发恢复。


### 6.2 管理员模块

*   验证 13 个客服均展示，概览数量和完成率正确。

*   验证时间、处理人、关键词筛选和倒序排列。

*   验证每页 50 条、20 条内部滚动、页码和总数展示。

*   验证当前页全选、清空选择、单条分配、批量分配和取消分配。

*   验证分配失败、部分失败、并发修改、空结果和加载失败。


## 7. 总体验收条件

1.  工单可统一接收访客留言和 service/support 邮箱来信，并正确形成回复链。

2.  每个功能点的正常流程、边界规则和异常处理均符合本文件定义。

3.  客服可完成查看、回复、抄送、转发、附件预览、视频播放、失败重试和处理状态更新。

4.  处理完毕可以不发送邮件；发送失败时正文、地址和附件不丢失。

5.  管理员可查看 13 个客服的处理概览，并按时间、处理人和关键词分配工单。

6.  管理员可单条或批量分配/取消分配，工单列表、客服概览和工作台显示一致。

7.  分配列表固定每页 50 条，达到 20 条后内部滚动，分页只显示总数和页码。

8.  同步失败、发送失败、预览失败、日期错误、分配失败和并发冲突均有明确反馈。

9.  账号权限、优先级和物流配送标签等明确排除项未纳入本期功能。
