# PRD：邮件工单模块

版本：V1.1  
状态：待评审

## 1. 概要

将访客端工单留言与客服邮箱来信统一进入客服工作台。客服可按邮件回复链查看客户问题、预览附件并通过邮件持续回复。

## 2. 联系人

| 角色 | 负责人 | 职责                               |
| ---- | ------ | ---------------------------------- |
| 产品 | 待定   | 范围确认与验收                     |
| 前端 | 待定   | 工单列表、回复链、富文本和附件预览 |
| 后端 | 待定   | 邮件同步、归链、发送及状态接口     |
| 测试 | 待定   | 正常、边界和异常场景验证           |

## 3. 背景

现有邮件查询接口只能读取邮件，无法完整支持访客留言入单、统一列表、已读状态、邮件发送和附件预览。需要将“一张工单”定义为“一条独立邮件回复链”。

## 4. 目标

- 访客留言和 service/support 邮箱来信进入同一工单列表。
- 客服能快速识别来源、已读状态和是否已发送回复。
- 客服可在回复链内查看附件并发送格式完整的邮件。
- 同一邮件不得重复入单，不同主题不得因邮箱相同而错误合并。

验收目标：工单来源和回复链归属准确；发送成功后列表状态即时更新；接口失败不丢失客服草稿。

## 5. 用户

- 网站访客：无法通过即时聊天解决问题时提交留言和邮箱。
- 一线客服：集中处理访客留言及多个客服邮箱收到的邮件。
- 客服主管：确认邮件是否读取、是否已发送回复及是否存在积压。

## 6. 核心规则

- **工单**：一次独立问题，对应一个 `ticket_id` 和一个 `thread_id`。
- **来源**：工单留言、service 邮箱、support 邮箱；邮箱来源取最近一封客户来信的收件邮箱。
- **状态**：仅分已读、未读。客户新留言或新邮件进入时为未读，客服打开后为已读。
- **回复状态**：最后一封邮件由客服发出时为已发送；客户再次回复后变为未发送。
- 本期不定义优先级、持续处理、处理完毕或关闭状态。

## 7. 功能需求

### 7.1 工单列表

- 每条固定展示：来源、收件日期、主题、买家邮箱、状态、回复状态。
- 支持近 7 天、1 个月、3 个月、6 个月及自定义时间。
- 支持按来源、问题类型、已读/未读及关键词筛选。
- 关键词匹配买家邮箱、主题、正文、工单号和订单号。
- 默认按最近一封客户来信的收件时间倒序排列。

### 7.2 回复链

- 按时间正序展示客户邮件和客服邮件。
- 每封展示发件人、收件人、主题、正文、时间、来源、附件和发送结果。
- 点击附件后在当前页面预览；图片直接查看，视频直接播放，PDF直接打开。
- 预览可通过关闭按钮、Esc或点击遮罩退出，不触发强制下载。

### 7.3 邮件回复

- 回复框默认缩小，点击图标展开或收起；收起不清空草稿。
- 默认带入客户邮箱、`Re:` 主题和当前来源邮箱。
- 正文支持换行、标题、字体、加粗、斜体、下划线、对齐、列表和链接。
- 邮件末尾固定追加品牌签名，签名不计入正文必填校验。
- 最多添加 5 个附件；发送成功后追加至回复链、回复状态改为已发送并收起回复框。

### 7.4 正常流程

1. 访客提交工单留言，或客户直接向 service/support 邮箱发信。
2. 服务端创建或匹配回复链，工单以未读、未发送状态进入列表。
3. 客服打开工单，状态改为已读并加载完整回复链。
4. 客服填写邮件并发送；成功后邮件追加到链尾，回复状态变为已发送。
5. 客户再次回复时，邮件追加到原链，工单置顶并变为未读、未发送。

### 7.5 边界与异常

- 同一买家邮箱可有多个工单，必须结合 `thread_id`、邮件 ID、主题和邮件头归链。
- 相同邮件 ID 只入库一次；接口重试和增量同步不得生成重复消息。
- 自定义开始日期不得晚于结束日期，查询包含起止日期全天。
- 邮件列表失败时保留旧数据；回复链失败时不得将缓存内容标记为完整。
- 发送失败或超时时保留正文和附件；超时先查发送结果，禁止直接重复发送。
- 预览地址失效时刷新签名地址；刷新失败仅提示预览失败，不自动下载。
- 富文本发送前清除脚本、事件属性及不安全链接，同时保留纯文本正文。
- 退信后回复状态不得显示已发送，应展示失败原因并允许修改收件邮箱后重试。

## 8. 发布与验收

首版包含统一列表、回复链、已读状态、邮件发送、富文本、固定签名和附件预览。实时推送可在首版使用短轮询，后续升级为 SSE/WebSocket。

验收标准：

- 三类来源显示和筛选准确，service/support 不混淆。
- 已读/未读及已发送/未发送按核心规则实时变化。
- 客户后续回复进入原链并置顶，不产生重复工单。
- 图片、视频和 PDF 可预览；预览失败不影响正文查看。
- 邮件发送失败、超时、退信时草稿和真实发送状态一致。
